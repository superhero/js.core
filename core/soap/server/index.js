class SoapServer
{
  constructor(soap)
  {
    this.soap = soap
  }

  listen(server, path, services, wsdl, callback)
  {

  }
}

module.exports = SoapServer


soap.listen()
soap.listen(server, '/wsdl', myService, xml)
